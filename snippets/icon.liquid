{% comment %}
	Renders a icon

	Accepts:
	- icon: {String} Name of icon (required)
	- class: {String} Classes

	Usage:
	{% render 'icon', icon: icon, class: class %}
{% endcomment %}

<img src="{{ request.asset_url }}/assets/icon/{{ icon }}.svg" class="svg-inline{% if class %} {{ class }}{% endif %}" alt="">
{%- assign sectionTitle = 'activate_account.title' | t -%}

{%- capture sectionContent -%}

	<p class="mb-0">
		{{ 'activate_account.description' | t }}
	</p>

{%- endcapture -%}

<section class="section section-activate-account">
    {% render 'section', sectionTitle: sectionTitle, sectionContent: sectionContent, sectionContentContainer: 'tiny', sectionOverlay: true %}
</section>